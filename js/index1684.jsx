/**
 * @param {string} allowed
 * @param {string[]} words
 * @return {number}
 */
var countConsistentStrings = function (allowed, words) {
  allowed = new Set(allowed);

  ddd
};

// setオブジェクトにallowedをいれる
// wordsの値を繰り返し処理して、ansに足していく（0でも、1でも）
// allowedの中にwordないのcが含まれていないなら、０、いるなら、１。
